public class Main {
    public static void main (String... args) {
        int[] newArray = {1, 20, 30, 232, 131};

        // Array.getLargestElement(newArray);

        // Array.reverseList(newArray);

        // Array.getExistingElements(newArray);

        Array.getPalindromeElements(newArray);
    }
}
